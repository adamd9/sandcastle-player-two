name: Auto-merge Agent PRs

on:
  pull_request:
    types: [ready_for_review]
    branches: [main]

permissions:
  contents: write
  pull-requests: write

jobs:
  auto-merge:
    if: |
      github.event.pull_request.user.login == 'copilot-swe-agent' ||
      github.event.pull_request.user.login == 'app/copilot-swe-agent'
    runs-on: ubuntu-latest
    steps:
      - name: Merge PR
        run: gh pr merge ${{ github.event.pull_request.number }} --squash --delete-branch
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Close linked issues
        run: |
          ISSUES=$(gh pr view ${{ github.event.pull_request.number }} \
            --repo ${{ github.repository }} \
            --json closingIssuesReferences \
            --jq '.closingIssuesReferences[].number')
          for ISSUE in $ISSUES; do
            gh issue close "$ISSUE" \
              --comment "✅ Implemented and merged via PR #${{ github.event.pull_request.number }}." \
              --repo ${{ github.repository }}
          done
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
