name: Player Two Turn

on:
  schedule:
    - cron: '30 * * * *'  # every hour, offset 30min from player one
  workflow_dispatch:

permissions:
  issues: write

jobs:
  take-turn:
    runs-on: ubuntu-latest
    steps:
      - name: Close previous turn issues
        run: |
          gh issue list --repo "$REPO" --label "player-turn" --state open \
            --json number --jq '.[].number' | \
          xargs -I{} gh issue close {} --repo "$REPO" --comment "Superseded by new turn" || true
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}

      - name: Create turn issue and assign Copilot
        run: |
          gh issue create \
            --repo "$REPO" \
            --title "Player turn: $(date -u '+%Y-%m-%d %H:%M UTC')" \
            --body "Take your turn in SandCastle Wars. Follow all instructions in .github/agents/sandcastle-player.agent.md — use the MCP tools to get_state, get_rules, then submit_move." \
            --label "player-turn" \
            --assignee copilot-swe-agent
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
